import NoResult from '.';

export default {
  title: 'shared/components/NoResult',
  component: NoResult,
};

const Template = (args) => <NoResult {...args} />;

export const DefaultNoResult = Template.bind({});
